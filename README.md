# B-13-A5-github

    - 1️⃣ What is the difference between var, let, and const?

    1 no asnwer
    var..........
    Old way to declare variable
    Can be re-declared and updated
    Function scoped

    let..........
    Modern way
    Can be updated but NOT re-declared
    Block scoped (inside {})


    const..........
    Cannot be updated or re-declared
    Used for fixed values
    Block scoped

    - 2️⃣ What is the spread operator (...)?

    2 no answer

    Spread operator (...) is used to expand or copy values.

    Example:
    ```js
    const arr1 = [1, 2, 3];
    const arr2 = [...arr1, 4, 5];

    - 3️⃣ What is the difference between map(), filter(), and forEach()?


    3 no answer
    map()

    Creates a new array
    
    Changes each element
    
    filter()
    
    Creates a new array
    
    Returns elements based on condition
    
    forEach()
    
    Just loops through array
    
    Does NOT return anything


    - 4️⃣ What is an arrow function?

    4 no answer

    Arrow function is a short way to write function.

     Example:
     
     const add = (a, b) => a + b;


    - 5️⃣ What are template literals?
    
    5 no answer

    Template literals are used to write strings easily using backticks ( ).
    Example:
    const name = "Sahadat";
     console.log(`Hello ${name}`);